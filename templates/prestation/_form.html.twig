{{ form_start(formPrestation, {attr: {'novalidate' : 'novalidate'}}) }}
{# {{form_row(formPrestation.image)}} #}
{{form_row(formPrestation.name)}}
{{form_row(formPrestation.content)}}
<button class="btn btn-success mb-1" type="submit">Valider</button>
{{ form_end(formPrestation) }}
